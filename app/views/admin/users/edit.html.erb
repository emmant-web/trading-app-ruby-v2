<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Trader</title>
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="m-0 h-screen w-screen overflow-hidden">
  <div class="flex h-screen w-full">
    <div>
      <%= render 'users/shared/adminSB' %>
    </div>

    <div class="flex-1 overflow-y-auto p-6 ml-64 bg-white">
      <div class="max-w-2xl w-full mx-auto p-6 bg-white rounded-xl shadow-md">
        <h1 class="text-2xl font-semibold mb-4 text-gray-800">Edit User</h1>

        <% if @user.errors.any? %>
          <div class="mb-4 border border-red-300 rounded p-4 bg-red-50">
            <h2 class="font-semibold text-red-700 mb-2">
              <%= pluralize(@user.errors.count, "error") %> prohibited this trader from being saved:
            </h2>
            <ul class="list-disc pl-5 text-red-600 text-sm">
              <% @user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <%= form_with model: [:admin, @user], class: "space-y-4" do |f| %>
          <div>
            <%= f.label :first_name, class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= f.text_field :first_name, class: "w-full rounded-lg border border-gray-300 px-4 py-2 text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-[#111111]" %>
          </div>

          <div>
            <%= f.label :last_name, class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= f.text_field :last_name, class: "w-full rounded-lg border border-gray-300 px-4 py-2 text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-[#111111]" %>
          </div>

          <div>
            <%= f.label :email, class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full rounded-lg border border-gray-300 px-4 py-2 text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-[#111111]" %>
          </div>

          <div>
            <%= f.label :password, class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= f.text_field :password,
              placeholder: "Leave blank to keep current password",
              autocomplete: "new-password",
              class: "w-full rounded-lg border border-gray-300 px-4 py-2 text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-[#111111]" %>
          </div>

          <div>
            <%= f.label :password_confirmation, class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= f.text_field :password_confirmation,
              placeholder: "Leave blank if password is blank",
              autocomplete: "new-password",
              class: "w-full rounded-lg border border-gray-300 px-4 py-2 text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-[#111111]" %>
          </div>

          <div>
            <%= f.label :balance, class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= f.number_field :balance, step: 0.01, class: "w-full rounded-lg border border-gray-300 px-4 py-2 text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-[#111111]" %>
          </div>
          
          <div>
            <%= f.select :is_pending,
              options_for_select([["Pending", true], ["Approved", false]], @user.is_pending),
              {},
              class: "rounded-lg border border-gray-300 px-4 py-2 text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-[#111111]" %>
          </div>

          <div>
            <%= f.select :is_admin,
              options_for_select([["Admin", true], ["Trader", false]], @user.is_admin),
              {},
              class: "rounded-lg border border-gray-300 px-4 py-2 text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-[#111111]" %>
          </div>

          <div class="flex items-center gap-4">
            <%= f.submit "Update User", class: "px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition-colors" %>
            <%= link_to "Back", admin_users_path, class: "text-sm text-blue-600 hover:underline" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</body>
</html>
