<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Pending Traders</title>
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="m-0 h-screen w-screen overflow-hidden">
  <div class="flex h-screen w-full">
    <div>
      <%= render 'users/shared/adminSB' %>
    </div>

    <div class="flex-1 overflow-y-auto p-6 ml-64 bg-white">
      <div class="max-w-6xl w-full mx-auto p-6 bg-white rounded-xl shadow-md">
        <h1 class="text-2xl font-semibold mb-4 text-gray-800">Pending Traders</h1>

        <div class="overflow-x-auto">
          <table class="min-w-full border text-sm text-gray-700">
            <thead>
              <tr class="bg-gray-100 text-left text-xs uppercase tracking-wider text-gray-600">
                <th class="px-4 py-3">Email</th>
                <th class="px-4 py-3">First Name</th>
                <th class="px-4 py-3">Last Name</th>
                <th class="px-4 py-3">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% @pending_users.each do |user| %>
                <tr class="border-b hover:bg-gray-50">
                  <td class="px-4 py-2 font-medium text-gray-800"><%= user.email %></td>
                  <td class="px-4 py-2"><%= user.first_name %></td>
                  <td class="px-4 py-2"><%= user.last_name %></td>
                  <td class="px-4 py-2 space-x-2">
                    <%= link_to "Details", admin_user_path(user),
                      class: "inline-block px-3 py-1 bg-blue-600 text-white rounded hover:bg-blue-700 transition-colors text-xs" %>
                    <%= link_to "Approve", approve_admin_user_path(user),
                      data: { turbo_method: :patch, turbo_confirm: "Approve this user?" },
                      class: "inline-block px-3 py-1 bg-green-600 text-white rounded hover:bg-green-700 transition-colors text-xs" %>
                    <%= link_to "Decline", admin_user_path(user),
                      data: { turbo_method: :delete, turbo_confirm: "Decline this user?" },
                      class: "inline-block px-3 py-1 bg-red-600 text-white rounded hover:bg-red-700 transition-colors text-xs" %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
