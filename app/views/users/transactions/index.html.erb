<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Desktop Sidebar</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="m-0 h-screen w-screen overflow-hidden">
    <div class="flex h-screen w-full">
      <div>
        <%= render 'users/shared/sidebar' %>
      </div>
      <div class="flex-1 flex flex-col overflow-y-auto p-6 ml-64 bg-white" id="buy-and-sell">
        <h1 class="text-3xl font-bold mt-4 mb-6 text-center">Transaction History</h1>
        <div class="max-w-6xl w-full mx-auto p-6 bg-white rounded-xl shadow-md border border-gray-200">
          <!-- Search Form -->
          <div class="mb-6 w-full max-w-5xl">
            <%= search_form_for @q, url: users_transactions_path, method: :get, html: { class: "flex space-x-4 items-center" } do |f| %>
              <%= f.search_field :symbol_cont, placeholder: "Search by symbol...", class: "border rounded-md px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-400" %>
              <%= f.select :action_eq,
                  options_for_select([["All Actions", ""], ["Buy", "buy"], ["Sell", "sell"]], selected: params.dig(:q, :action_eq)),
                  {}, class: "border px-3 py-2 rounded text-sm" %>
              <%= f.submit "Search", class: "bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-md text-sm" %>
              <%= link_to "Clear", users_transactions_path, class: "bg-rose-500 hover:bg-rose-600 text-white px-4 py-2 rounded-md text-sm" %>
            <% end %>
          </div>
          <div class="overflow-x-auto border border-gray-200 rounded-xl">
            <table class="min-w-full text-sm text-gray-700">
              <thead>
                <tr class="bg-gray-100 text-left text-xs uppercase tracking-wider text-gray-600">
                  <th class="px-4 py-3 rounded-tl-xl">Symbol</th>
                  <th class="px-4 py-3">Shares</th>
                  <th class="px-4 py-3">Action</th>
                  <th class="px-4 py-3">Price ($)</th>
                  <th class="px-4 py-3 rounded-tr-xl">Total ($)</th>
                </tr>
              </thead>
              <tbody>
                <% @transactions.each do |t| %>
                  <tr class="hover:bg-gray-50">
                    <td class="px-4 py-2 font-medium text-gray-800 rounded-bl-xl"><%= t.symbol.upcase %></td>
                    <td class="px-4 py-2"><%= t.quantity %></td>
                    <td class="px-4 py-2">
                      <span class="<%= t.action == 'buy' ? 'text-green-600' : 'text-red-600' %> font-semibold">
                        <%= t.action.capitalize %>
                      </span>
                    </td>
                    <td class="px-4 py-2">$<%= number_with_precision(t.stock_price, precision: 2) %></td>
                    <td class="px-4 py-2 rounded-br-xl">$<%= number_with_precision(t.total_price, precision: 2) %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <div class="mt-4 flex justify-center">
            <%= paginate @transactions, theme: nil %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
