<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portfolio</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="m-0 h-screen w-screen overflow-hidden bg-white text-gray-800">
    <div class="flex h-screen w-full">
      <div>
        <%= render 'users/shared/sidebar' %>
      </div>

      <div class="w-screen h-screen flex flex-col items-center ml-64 px-10 py-8 overflow-y-auto">
        <h1 class="text-3xl font-bold mt-4 mb-6">Your Portfolio</h1>

        <div id="card-container" class="border border-gray-200 px-6 py-6 shadow-lg rounded-lg w-full min-h-[20rem] max-w-6xl mx-auto flex flex-col items-center">

          <!-- Balance Card -->
          <div class="w-full max-w-5xl bg-[#fafafa] border border-gray-200 rounded-lg p-6 mb-12">
            <h2 class="text-sm font-medium text-gray-500 uppercase tracking-wide">Available Balance</h2>
            <p class="text-3xl font-bold text-emerald-600 mt-1">
              $<%= number_with_precision(current_user.balance, precision: 2) %>
            </p>
          </div>

          <!-- Search Form -->
          <div class="mb-6 w-full max-w-5xl">
            <%= search_form_for @q, url: users_portfolio_path, method: :get, html: { class: "flex space-x-4 items-center" } do |f| %>
              <%= f.search_field :symbol_cont, placeholder: "Search by symbol...", class: "border rounded-md px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-400" %>
              <%= f.submit "Search", class: "bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-md text-sm" %>
              <%= link_to "Clear", users_portfolio_path, class: "bg-rose-500 hover:bg-rose-600 text-white px-4 py-2 rounded-md text-sm" %>
            <% end %>
          </div>

          <!-- Holdings Table -->
          <div class="w-full max-w-5xl border border-gray-200 rounded-lg overflow-hidden">
            <% if @holdings.present? && @holdings.any? %>
              <table class="table-auto w-full bg-white text-sm">
                <thead>
                  <tr class="bg-gradient-to-r from-blue-100 to-blue-200 text-blue-900 font-bold">
                    <th class="px-4 py-3 text-left rounded-tl-lg">Symbol</th>
                    <th class="px-4 py-3 text-left">Shares</th>
                    <th class="px-4 py-3 text-right rounded-tr-lg">Total Price</th>
                  </tr>
                </thead>
                <tbody>
                  <% @holdings.each do |holding| %>
                    <tr class="hover:bg-blue-50">
                      <td class="px-4 py-3"><%= holding.symbol %></td>
                      <td class="px-4 py-3 text-left"><%= holding.shares %></td>
                      <td class="px-4 py-3 text-right">
                        $<%= number_with_precision(holding.total_price, precision: 2) %>
                      </td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            <% else %>
              <p class="text-gray-600 m-6 text-md">You don’t own any stocks yet.</p>
            <% end %>
          </div>
          
          <!-- Pagination -->
          <div class="w-full max-w-5xl mt-4">
            <div class="flex justify-center">
              <%= paginate @holdings, theme: nil %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
